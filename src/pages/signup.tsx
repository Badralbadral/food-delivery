import { Stack } from "@mui/material";
import { SignupForm } from "@/components/Signup";

const Signup = () => {
  return (
    <Stack pt={15}>
      <SignupForm />
    </Stack>
  );
};
export default Signup;
