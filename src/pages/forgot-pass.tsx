import { StepTree } from "@/components/Forgot Password";
import { Stack } from "@mui/material";

const forgotPass = () => {
  return (
    <Stack mt={16}>
      <StepTree />
    </Stack>
  );
};
export default forgotPass;
