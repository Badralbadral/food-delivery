import { Header } from "@/components/Header";
import { Stack } from "@mui/material";

const Home = () => {
  return (
    <Stack justifyContent={"center"}>
      <Header />
    </Stack>
  );
};

export default Home;
