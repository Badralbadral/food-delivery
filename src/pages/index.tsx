import { Stack } from "@mui/material";
import { Home } from "@/components/Home";

const HomePage = () => {
  return (
    <Stack mt={`51px`}>
      <Home />
    </Stack>
  );
};

export default HomePage;
