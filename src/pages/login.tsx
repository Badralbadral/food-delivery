import { LoginForm } from "@/components/Login";
import { Stack } from "@mui/material";

const Login = () => {
  return (
    <Stack py={15}>
      <LoginForm />
    </Stack>
  );
};

export default Login;
