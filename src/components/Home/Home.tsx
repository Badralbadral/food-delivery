import { Stack } from "@mui/material";
import { HeroBanner } from "./HeroBanner";

export const Home = () => {
  return (
    <Stack width={1440} height={788}>
      <HeroBanner />
    </Stack>
  );
};
